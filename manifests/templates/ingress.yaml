---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mini-project-5eec03f2                  # Ingress name
  labels:
    app: mini-project-5eec03f2
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "mini-project-5eec03f2.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mini-project-5eec03f2               # Links to Service name dynamically
                port:
                  number: 3000  # Links to service port dynamically
